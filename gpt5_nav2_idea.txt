local_costmap:
  local_costmap:
    ros__parameters:
      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true
      width: 8.0
      height: 8.0
      resolution: 0.05
      footprint_clearing_enabled: true
      plugins: ["voxel_layer", "inflation_layer"]

      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: true
        # 3D voxel grid bounds — make sure these include your sensor height
        origin_z: 0.0
        z_voxels: 16
        z_resolution: 0.1        # -> z max ≈ 1.6 m
        max_obstacle_height: 1.8
        publish_voxel_map: false
        # usual ranges
        obstacle_range: 4.0
        raytrace_range: 6.0

        observation_sources: depth_clear depth_mark

        depth_clear:
          topic: /camera/depth/points
          sensor_frame: camera_link
          clearing: true
          marking: false
          observation_persistence: 0.0    # <-- no stale origins for raytracing
          expected_update_rate: 15.0      # (optional, warns if slower)

        depth_mark:
          topic: /camera/depth/points
          sensor_frame: camera_link
          clearing: false
          marking: true
          observation_persistence: 0.5    # <-- short memory to stabilize
